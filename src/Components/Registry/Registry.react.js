import React from 'react';

// Styles
import { OuterOutline, InnerOutline } from '../Shared/Styles/Shared.styled';

const Registry = () => {
  return (
    <div className='events-page'>
      <h1 className='invited'>We're just not ready to make these kinds of decisions yet!</h1>
      <OuterOutline>
        <InnerOutline>
          <h1>Registry Info Coming Soon...</h1>
          <h3>Please check back sometime after the new year</h3>
        </InnerOutline>
      </OuterOutline>
    </div>
  );
}

export default Registry;