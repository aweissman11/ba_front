import styled from 'styled-components';
import { Colors } from '../Shared/Styles/Colors';

export const RegistryWrapper = styled.div`
  body {
    background-color: ${Colors.BG};
  }
`;
RegistryWrapper.displayName = 'RegistryWrapper';